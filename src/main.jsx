import React from "react";
import ReactDOM from "react-dom/client";
import PostureApp from "../PostureCorrection_v3.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PostureApp />
  </React.StrictMode>
);